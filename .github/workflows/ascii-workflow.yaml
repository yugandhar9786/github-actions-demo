name: ascii-generation workflow
on: push
jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v2
            - name: executing shell-script
              run: ./github/workflows/ascii.sh
              
            - name: list repo files
              run: ls -la
              
            